for (let i =0; i < 5; i++) {
console.log("Hello")
console.log('World');
}
function greet ("name" = name);

let myObject ={
	X:5,
	y: 10,
	product: function() {
		return this.x * this
	},
};

let myfunlong = function(x,y){
	return x * y;
}

let myfunc = (x, y) => x * y

alert(myfunc(10,17));
}